
description = 'lakeside-web'
dependencies {
    compile group: 'org.springframework', name: 'spring-core', version:'3.1.2.RELEASE'
    compile group: 'org.springframework', name: 'spring-context', version:'3.1.2.RELEASE'
    compile group: 'org.springframework', name: 'spring-web', version:'3.1.2.RELEASE'
    compile group: 'org.springframework', name: 'spring-webmvc', version:'3.1.2.RELEASE'
  compile project(':lakeside-core')
    compile group: 'org.apache.shiro', name: 'shiro-core', version:'1.2.1'
    compile group: 'org.apache.shiro', name: 'shiro-ehcache', version:'1.2.1'
    compile group: 'org.apache.shiro', name: 'shiro-spring', version:'1.2.1'
    compile group: 'org.apache.shiro', name: 'shiro-web', version:'1.2.1'
    compile(group: 'javax.servlet', name: 'servlet-api', version:'2.5') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'javax.servlet.jsp', name: 'jsp-api', version:'2.2') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
}
